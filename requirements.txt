# Core LLM
anthropic>=0.40.0
openai>=1.30.0

# Embeddings
sentence-transformers>=3.0.0

# Vector Store (local)
chromadb>=0.5.0

# Hybrid retrieval (BM25 + vector RRF)
rank-bm25>=0.2.2

# Schema / config parsing
pyyaml>=6.0.0

# ── Azure Knowledge Base ──────────────────────────────────────
# Azure AI Search  (vector + BM25 + semantic reranking index)
azure-search-documents>=11.4.0

# Azure Identity  (managed identity, DefaultAzureCredential, az login)
azure-identity>=1.15.0

# Azure Blob Storage  (optional: raw file backup)
azure-storage-blob>=12.19.0

# Document Loaders (optional, install as needed)
pdfplumber>=0.11.0
python-docx>=1.1.0
beautifulsoup4>=4.12.0

# API Server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Utilities
python-dotenv>=1.0.0
